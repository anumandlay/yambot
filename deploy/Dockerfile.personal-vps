# Isolated personal VPS manager — Docker socket only; not YamBot agents.
FROM node:24-bookworm-slim
WORKDIR /app
COPY personal-vps/package.json ./
RUN npm install --omit=dev
COPY personal-vps/src ./src
ENV NODE_ENV=production
CMD ["node", "src/index.js"]
