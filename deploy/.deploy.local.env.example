# Local-only SSH deploy credentials (NEVER commit).
# Copy to deploy/.deploy.local.env and fill in.

YAMBOT_SSH_HOST=15.204.242.239
YAMBOT_SSH_USER=ubuntu
YAMBOT_SSH_PASSWORD=

# Optional site-wide LLM seeded on API boot (keep key out of git)
DEFAULT_LLM_BASE_URL=https://api.minimax.io/v1
DEFAULT_LLM_MODEL=MiniMax-M2.7
DEFAULT_LLM_API_KEY=

# Optional: Vercel AI Gateway for Jev (Auto reply vs queue_goal). Never commit real keys.
AI_GATEWAY_API_KEY=
# JEV_ENABLED=
# JEV_MODEL=typesafe-ai/jev

# Optional: Composio Phase-1 (Gmail/Slack/Sheets). Never commit real keys.
# COMPOSIO_API_KEY=
# COMPOSIO_ENABLED=1
