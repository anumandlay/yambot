# Auto-provisions per-agent Playwright containers via Docker socket.
FROM node:24-bookworm-slim
WORKDIR /app
COPY computer-manager/package.json ./
RUN npm install --omit=dev
COPY computer-manager/src ./src
ENV NODE_ENV=production
CMD ["node", "src/index.js"]
